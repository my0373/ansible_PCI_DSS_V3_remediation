---
- hosts:  foreman_hostgroup_rhel7
  remote_user: ansible
  gather_facts: yes


  tasks:
    - name: Open HTTP firewall port 80
    firewalld:
      service: http
      permanent: true
      state: enabled
      immediate: yes